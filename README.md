"# Анализатор отчетов Яндекс.Директ
Запуск скрипта: 
python yandex_direct_analyzer.py

## Быстрый запуск

1. **Настройте скрипт:**
   - Откройте `yandex_direct_analyzer.py`
   - В начале файла найдите блок настроек и укажите:
     ```python
     REPORT_TITLE = "Отчет по сайту Опора за период 1 марта - 04 апреля 2026г."
     SITE_NAME = "opora"  # Название сайта для поиска файлов
     ```

2. **Подготовьте файлы данных:**
   - Поместите Excel-файлы в папку `data/`
   - Обязательный файл: `mainstat_{SITE_NAME}*.xlsx`
   - Опциональные файлы:
     - `regions_{SITE_NAME}*.xlsx` - данные по регионам
     - `polvozrast_{SITE_NAME}*.xlsx` - данные по полу и возрасту
     - `pfrases_{SITE_NAME}*.xlsx` - данные по поисковым фразам

3. **Запустите скрипт:**
   ```powershell
   python yandex_direct_analyzer.py
   ```

4. **Получите результаты:**
   - Excel-отчет: `out/{SITE_NAME}_analysis_result.xlsx`
   - TXT-файл с рекомендациями: `out/{SITE_NAME}_recommendations.txt`

---

## Анализ отчётов Яндекс.Директ из Excel
Скрипт на Python для автоматического анализа Excel‑отчётов из Яндекс.Директ: считает ключевые метрики (CPC, CPA, конверсия), строит сводки по кампаниям и основным срезам, выводит текстовый отчёт в терминал и формирует Excel‑файл с результатами.

## Возможности

### Загрузка данных
- Автоматический поиск файлов по паттернам в папке `data/`
- Поддержка множественных источников данных (mainstat, regions, polvozrast, pfrases)
- Умный пропуск служебных строк из выгрузок Яндекс.Директ
- Автоматическое определение и нормализация колонок

### Анализ данных
- **Расчёт метрик:** Расход, Клики, Конверсии, CPC, CPA, CR, CTR
- **Определение типа кампании:** Поиск, РСЯ, Товарная, Смарт-баннеры, Динамическая
- **Анализ по срезам с группировкой по кампаниям:**
  - Тип кампании
  - Устройство
  - Пол и возраст (объединенные)
  - Тип условия показа
  - Площадки (с учетом возможности блокировки)
  - Группы объявлений
  - Заголовки
  - Изображения

### Формирование отчетов

**Excel-файл с вкладками:**
- Обзор (общая статистика)
- Кампании
- Тип кампании
- Рекомендации
- Устройство, Пол и возраст, Тип условия показа, Площадка, Группа, Заголовок, Изображение
- На каждой вкладке:
  - Заголовок отчета (A1, шрифт 18)
  - Название вкладки (A2, шрифт 16)
  - Таблица с данными и метриками

**TXT-файл с рекомендациями:**
- Структурированные рекомендации по каждой кампании
- Списки площадок для минусовки
- Пометки о невозможности отключения площадок

## Требования
- Python 3.9+
- Зависимости указаны в `requirements.txt`.

## Установка
1. Клонируйте репозиторий или скачайте файлы проекта.
2. Создайте и активируйте виртуальное окружение:
   ```shell
   # Windows
   python -m venv venv
   venv\\Scripts\\activate
   
   # macOS / Linux
   python3 -m venv venv
   source venv/bin/activate
   ```
3. Установите зависимости:
   ```shell
   pip install -r requirements.txt
   ```
4. Убедитесь, что у вас есть папки `data/` и `out/`. Если нет, создайте их.

## Запуск

### Вариант 1: Настройка в коде (рекомендуется)
1. Откройте файл `yandex_direct_analyzer.py`
2. В самом начале файла найдите блок настроек:
   ```python
   # ==================== НАСТРОЙКИ ====================
   DEFAULT_INPUT_FILE = "data/BD_RSY_MK_BFL_OPORA_01.03-04.04 2026.xlsx"
   DEFAULT_OUTPUT_FILE = "out/analysis_result.xlsx"
   # ===================================================
   ```
3. Измените `DEFAULT_INPUT_FILE` на путь к вашему файлу
4. Измените `DEFAULT_OUTPUT_FILE` на желаемое имя выходного файла
5. Запустите скрипт:
   ```shell
   python yandex_direct_analyzer.py
   ```

**Важно:** Вы можете хранить несколько файлов в папке `data/`, но для анализа нужно указать конкретный файл в настройках.

### Вариант 2: Запуск через командную строку
Если вы не хотите редактировать код, можно указать файлы через аргументы:

### Расширенный запуск
Вы можете указать пути к файлам и другие параметры через аргументы командной строки:

- `--input`: Путь к файлу или папке с файлами (по умолчанию: `data`).
- `--output`: Путь к итоговому Excel-файлу (по умолчанию: `out/analysis_{дата_время}.xlsx`).
- `--min-spend`: Минимальный расход для попадания сегмента в рекомендации.
- `--min-clicks`: Минимальное количество кликов для анализа сегмента.

**Пример:**
```shell
python yandex_direct_analyzer.py --input "C:\reports\my_report.xlsx" --output "C:\reports\analysis.xlsx" --min-spend 1000 --min-clicks 50